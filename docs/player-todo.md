# Player To-Do

## Deferred preferences

- [ ] Revisit independent Music and Cinema sequence/random playback behavior. Current preference: each player makes its own sequence-versus-random decisions; after the last item in a numbered series, that player enters random playback, and a random selection associated with a numbered sequence restarts numeric playback. The tagging model, restart boundary, and exact random-selection rules are intentionally unresolved and require a separate design discussion.
